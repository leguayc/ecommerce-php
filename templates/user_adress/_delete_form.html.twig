<form method="post" action="{{ path('user_adress_delete', {'id': user_adress.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_adress.id) }}">
    <button class="btn">Delete</button>
</form>
